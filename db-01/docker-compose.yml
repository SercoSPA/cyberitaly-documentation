# ci-mon-db-01 — Elasticsearch single node
# 4 vCPU / 16 GB RAM / 100 GB on /data
#
# Deploy:  cd db-01 && docker compose up -d
# Requires: ../certs copied here OR symlinked, and .env filled in.
services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:${STACK_VERSION}
    container_name: elasticsearch
    restart: unless-stopped
    environment:
      - node.name=ci-mon-db-01
      - cluster.name=${CLUSTER_NAME}
      - discovery.type=single-node
      - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
      - bootstrap.memory_lock=true
      # Heap = ~50% of the 16 GB host RAM
      - ES_JAVA_OPTS=-Xms8g -Xmx8g
      - xpack.license.self_generated.type=${LICENSE}
      # --- Security ---
      - xpack.security.enabled=true
      # HTTP TLS (clients: Kibana, Fleet, Heartbeat, k8s agents)
      - xpack.security.http.ssl.enabled=true
      - xpack.security.http.ssl.key=certs/elasticsearch/elasticsearch.key
      - xpack.security.http.ssl.certificate=certs/elasticsearch/elasticsearch.crt
      - xpack.security.http.ssl.certificate_authorities=certs/ca/ca.crt
      # Transport TLS (required even for a single node)
      - xpack.security.transport.ssl.enabled=true
      - xpack.security.transport.ssl.key=certs/elasticsearch/elasticsearch.key
      - xpack.security.transport.ssl.certificate=certs/elasticsearch/elasticsearch.crt
      - xpack.security.transport.ssl.certificate_authorities=certs/ca/ca.crt
      - xpack.security.transport.ssl.verification_mode=certificate
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    mem_limit: 12g
    volumes:
      - /data/elasticsearch:/usr/share/elasticsearch/data
      - ../certs:/usr/share/elasticsearch/config/certs:ro
    ports:
      - "9200:9200"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -s --cacert config/certs/ca/ca.crt https://localhost:9200 | grep -q 'missing authentication credentials'",
        ]
      interval: 15s
      timeout: 10s
      retries: 20
